using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace FaceRecognition
{
    class Class1
    {
        //connect();
        //cmd = new MySqlCommand("SELECT * FROM tbl_need WHERE needID=@fd1", con);
        //cmd.Parameters.AddWithValue("@fd1", "1");
        //dr = cmd.ExecuteReader();
        //if ((dr.Read()))
        //{
        //    //txtIDNumber.Text = (dr["IDNumber"].ToString());
        //    //txtname.Text = (dr["name"].ToString());
        //    comportno = (dr["comportno"].ToString());
        //    cmd.Dispose();
        //    return;
        //}

    }
}
